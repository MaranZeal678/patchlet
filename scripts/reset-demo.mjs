#!/usr/bin/env node
/**
 * Placeholder. The demo reset restores the host application repository to its pre-demo state and
 * clears the previous run's conversations, escalations and trace events. See docs/demo.md.
 */
console.error("demo:reset is not implemented yet. See docs/demo.md for the manual steps.");
process.exit(1);
