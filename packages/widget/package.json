{
  "name": "@patchlet/widget",
  "version": "0.1.0",
  "private": true,
  "description": "Embeddable Patchlet support widget: chat, affordance scan and on-page guidance.",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build && npm run postbuild",
    "postbuild": "node scripts/copy-dist.mjs",
    "dev": "concurrently -k -n build,mock -c cyan,magenta \"vite build --watch --mode development\" \"tsx dev/mock-api.ts\"",
    "mock": "tsx dev/mock-api.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dom-accessibility-api": "^0.7.0",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "jsdom": "^25.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5",
    "@types/node": "^22.9.0"
  }
}
