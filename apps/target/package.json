{
  "name": "@patchlet/target",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Meridian Supply — the demo commerce admin whose users unknowingly author its agent API. Serves the app, its real HTTP API, and the Action Compiler recorder widget.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
